use shida_data_directory::DataDirectory;

pub struct PythonVirtualEnvironment;

impl PythonVirtualEnvironment {
    pub fn new(
        // configuruation: PythonVirtualEnvironmentConfiguration,
        data_directory: DataDirectory,
    ) -> Self {
        // let virtualenv_directory_path = data_directory.get_sub_path(VIRTUALENV_DIRECTORY_SUB_PATH);
        Self
    }
}
